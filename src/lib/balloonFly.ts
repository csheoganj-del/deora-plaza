/**
 * balloonFly - DISABLED
 * Balloon animations have been disabled per user request
 */
export function balloonFly(
    sourceEl: HTMLElement | null,
    targetEl: HTMLElement | null,
    size: number = 60
): void {
    // Balloon animation disabled
    return;
}

/**
 * balloonReturn - DISABLED
 * Balloon animations have been disabled per user request
 */
export function balloonReturn(
    sourceEl: HTMLElement | null,
    targetEl: HTMLElement | null
): void {
    // Balloon animation disabled
    return;
}

/**
 * balloonDeflate - DISABLED
 * Balloon animations have been disabled per user request
 */
export function balloonDeflate(sourceEl: HTMLElement | null) {
    // Balloon animation disabled
    return;
}
